[alias]
    au = add -u .
    co = checkout
    dh = diff HEAD
    st = status
    pom = push origin master
    update-submodules = submodule update --init --depth 100 --recursive
    upgrade-submodules = submodule update --init --depth 100 --remote

[color]
    ui = auto

[color "grep"]
    match = cyan bold
    selected = blue
    context = normal
    filename = magenta
    linenumber = green
    separator = yellow
    function = blue

[user]
    name = Riza Suminto
    email = riza.on@gmail.com

[push]
    default = simple

[include]
    path = ~/.gitconfig_local

[core]
    excludesfile = ~/.gitignore_global
