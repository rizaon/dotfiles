- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.emacs:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/bin:

- shell:
    - [git submodule update --init --depth 1 --recursive, Installing submodules]
